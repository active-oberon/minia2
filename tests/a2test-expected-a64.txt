# Cases known to fail when the suites are compiled for AArch64 (`ob test -t a64`).
# Format: <TestFile><TAB><kind>: <case name>.  A listed case that PASSES is reported
# as FIXED and fails the run, so this file cannot silently go stale.
#
# The four entries this file shares with a2test-expected.txt are the same defects for
# the same reasons -- no procedure overloading, value parameters of non-static math
# arrays forbidden on purpose, a case with stale 32-bit assumptions, and the math array
# literal pooling bug -- and the reasoning for each is written out there rather than
# repeated here.
#
# --- also failing on x86-64, see tests/a2test-expected.txt --------------------------
MathArrays.Compilation.Test	positive: use of COMPLEX32 and COMPLEX64
MathArrays.Compilation.Test	positive: variables and parameters of math array type
MathArrays.Execution.Test	positive: correct use of ADDRESS OF and SYSTEM.VAL for tensors and open math arrays
MathArrays.Execution.Test	positive: driver tests (fill and compare arrays)
#
# --- inline assembly written for another processor ----------------------------------
# Both declare `CODE{SYSTEM.i386}` with x86 mnemonics in it. There is nothing for an
# AArch64 assembler to do with that, and it says so ("inline code is declared for a
# processor this assembler does not know") rather than reporting a parse error about a
# construct spelled as intended. They pass on x86-64, where the code is the
# architecture's own; the same two fail on ARM32 for the same reason.
Oberon.Compilation.Test	positive: call an empty inline code
Oberon.Compilation.Test	positive: export inline code with reference to (local) constant
#
# --- not in the AArch64 library ----------------------------------------------------
# The case imports JPEGEncoder, and the A64 library is the headless set
# (docker/headless-core.txt), which does not carry it. That is a scope limit of the
# library, not a defect of the target -- the same case is noted as failing in the SDK
# image for the same reason.
JPEG.Execution.Test	positive: test RGB to I420 2x2x8x8 block conversion used for encoding
#
# The eight cases that used to stand here -- three in Texts.Test over TextUtilities, five in
# ImageCodecs.Test over BMPCodec/GIFCodec/PictCodec -- pass since 2026-08-24 and are gone from this
# file. The trade they were listed under has turned around: configs/moduleListA64.txt is now derived
# as the payload PLUS what the suites import, closed over the import graph, instead of being an
# ageing superset. That took the AArch64 objects from 407 modules to 350 -- so the shipped tarball
# got smaller, and these eight got the modules they were missing. Both at once, which is why the
# note about "growing a shipped artifact so that five cases can run" no longer applies.
#
# --- written for an instruction set this one is not ---------------------------------
# The case imports Shortreal, which is AMD64.Shortreal.Mod: half precision floats
# through the AVX F16C instructions, and one of the four modules configs/moduleListA64.txt
# names as unable to exist on this architecture. AArch64 has half precision of its own
# (FEAT_FP16), so a Shortreal for it is writable; there is none, and until there is, the
# one case of this suite has nothing to import.
Shortreal.Execution.Test	positive: test scalar SHORTREAL operators

# --- the fallback logarithm ---------------------------------------------------------
# On x86 `ln` is one FYL2X instruction; everywhere else it is the series and the mantissa
# decomposition below it in Math64.Mod, and that is what AArch64 compiles. Measured against
# known values: ln(2) is 1.9E-9 out, ln(50) 9.5E-9, ln(1E6) 4.4E-7 -- growing with the
# argument, where double precision should give 1E-16. `exp` is exact to 1E-12 on the same
# machine, so this is the logarithm alone, and anything built on it inherits the error.
# The case below checks exp(ln(x)) = x to 1E-9 and therefore fails here. It is left failing
# on purpose: the number is what a math library owes, and lowering the test to fit the
# implementation would hide it.
MathFunctions.Test	positive: a logarithm and an exponent undo each other

# --- atomics under an emulator: settled 07.09.2026, and the entry is gone -----------
# Two activities adding one to the same counter used to come to about 1270 here instead
# of 2000, and this file said the question would be settled by running the case on a
# processor rather than on an emulator. It was: on a Galaxy S24 (Snapdragon 8 Gen 3) the
# case passes, and it passes under this machine's qemu now as well -- so the lost
# additions were the emulator's exclusive monitor and not the ldaxr/stlxr loop. Nothing
# to list; the case is expected to pass everywhere.

# --- a report of forty thousand characters, under an emulator --------------------------
# TrapWriters.Test's overflow case writes 40000 characters through a trap writer, and every
# one of them also goes to Trace.Char by design. Native and on a phone it passes; under
# qemu-user the image prints the head of the report and then stops without reaching the
# "Test loaded." marker and without trapping, so the harness has nothing to call a pass.
# What kills it has not been found -- it is not the case timeout (600s changes nothing) and
# not the harness's error-line heuristic (removed 07.09). Listed rather than weakened: the
# case is about a defect worth keeping in front of us (a report longer than the buffer is
# silently cut), and it does that job on every other host.
TrapWriters.Test	positive: a report too long for the buffer keeps its head and loses its tail, and says nothing about it

# --- the two Windows suites, on this host too ------------------------------------------
# tests/WinFS.Test and tests/WinTraps.Test are about the Windows twins of Files and Traps,
# and the modules they name are not in an AArch64 build at all -- the same reason they are
# listed in tests/a2test-expected.txt, where it is written out. Eleven of the thirty-six
# cases in those two files pass here as they do on Linux (the shared modules, and what is
# not about the host) and are deliberately not listed.
#
# Measured 2026-09-08 on a Galaxy S24, natively: 7615 cases, 7580 passed, 25 failed --
# these -- and 9 known. The TrapWriters entry below is listed for qemu only, so the
# on-device script filters that section out before it runs (tests/a64-device-suite.sh).
WinFS.Test	positive: a file that was never registered has no date to give, and says so
WinFS.Test	positive: a name too long for what it is copied into is cut, not written past
WinFS.Test	positive: a relative name with a directory in it is looked for along the search path
WinFS.Test	positive: a rider that was never given a position reads from the beginning of the file
WinFS.Test	positive: an absolute name goes in through the drive it names, and comes back the same file
WinFS.Test	positive: and a name that fits comes back relative to where we stand
WinFS.Test	positive: the attributes of a name, and of a name that is not there
WinFS.Test	positive: the characters a file name may not contain
WinFS.Test	positive: the two directories the process was given, and neither of them is empty
WinFS.Test	positive: the working directory is where the harness put us, and can be moved and moved back
WinFS.Test	positive: two names that differ only in case are one name here
WinTraps.Test	positive: a HALT the program wrote itself is named a HALT, and an unknown number is not
WinTraps.Test	positive: a processor fault is named by what the processor said, and carries no halt code
WinTraps.Test	positive: a trap the debugger does not answer for is still asked about only once
WinTraps.Test	positive: a trap under a FINALLY is reported, and the activity runs on past it
WinTraps.Test	positive: an activity that traps is reported, and the case that started it lives on
WinTraps.Test	positive: and about a fault, where there is no halt code to tell
WinTraps.Test	positive: and so do the numbers the system raises on its own
WinTraps.Test	positive: the account begins and ends with the markers that say where it begins and ends
WinTraps.Test	positive: the level in the first line counts the traps that are still being reported
WinTraps.Test	positive: the numbers the compiler plants have the words the compiler means by them
WinTraps.Test	positive: the registers are there, and by the names this handler calls them
WinTraps.Test	positive: the report says which system and when, before it says anything else
WinTraps.Test	positive: two activities that trap one after the other get one report each
WinTraps.Test	positive: what a debugger is told about a trap it did not plant
