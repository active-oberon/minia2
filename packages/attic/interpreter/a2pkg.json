{
  "name": "attic/interpreter",
  "tier": null,
  "context": "A2",
  "headless": false,
  "status": "unsupported",
  "description": "An interpreting back end: runs the intermediate code instead of generating machine code.",
  "provides": [
    "FoxInterpreter",
    "FoxInterpreterBackend",
    "FoxInterpreterSymbols"
  ],
  "requires": {
    "std/base": "*",
    "std/compiler": "*",
    "std/data": "*",
    "std/runtime": "*",
    "std/system": "*"
  },
  "residual": [
    "Interesting and unused. If it were ever revived it would want a test suite first -- it has none, and neither does anything else in this area."
  ]
}
