{
  "name": "std/drivers",
  "tier": 5,
  "context": "A2",
  "headless": true,
  "status": "draft",
  "description": "Low-level device drivers: display registry, serial ports (V24), keyboard/mouse, PC speaker, clipboard.",
  "provides": [
    "A2Sequencers",
    "AndroidDisplay",
    "AndroidInput",
    "Beep",
    "Clipboard",
    "Display",
    "DisplayGTF",
    "DisplayNull",
    "Displays",
    "HostClipboard",
    "Joysticks",
    "KbdMouse",
    "RAWPrinter",
    "Serials",
    "SerialsVirtual",
    "ShellSerial",
    "TVDriver",
    "V24",
    "V24Tracer"
  ],
  "graphical": [
    "AndroidDisplay",
    "AndroidInput",
    "Display",
    "DisplayNull",
    "Displays",
    "KbdMouse"
  ],
  "requires": {
    "std/base": "*",
    "std/math": "*",
    "std/runtime": "*",
    "std/system": "*",
    "std/text": "*"
  },
  "residual": [
    "The modules under `graphical` belong to this package but cannot travel in the headless SDK: their own import closure reaches the window system. The list is checked against the import graph by docker/gen-headless-core.sh, so it cannot go quietly out of date.",
    "Library cores (Displays, Serials) resolve to std/runtime only. The rest are thin device shims.",
    "WARNING: Beep is intentionally NOT included — Unix Beep imports X11/Displays (see the DataErrors fix). If a real cross-platform PC-speaker driver is wanted it needs a headless backend first.",
    "App/tool members of the driver family (BootManager, HotKeys, CPUID, V24Tracer, TVDriver, VfW, WSock32) are commands/apps -> packages/apps, not this library.",
    "Six devices entered 2026-08-24, unclaimed until then: a joystick, the generic sequencer (negelef), a serial-line tracer, a raw printer, the serial shell and the TV tuner. All compile headless: a driver in this system is a module, not a kernel privilege.",
    "AndroidDisplay and AndroidInput (ours, out of the Android phase) and Beep entered 2026-08-24. The two Android ones are marked graphical: they ARE a display and an input device."
  ]
}
